# BA-Agent Requirements
# 使用清华 PyPI 镜像源安装: pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# ============ Core dependencies ============
# API 认证与表单 (backend.api.main：JWT、密码校验、文件上传)
passlib[sha256_crypt]>=1.7.4
PyJWT>=2.8.0
python-multipart>=0.0.9
langchain>=0.3.0
langchain-core>=1.2.0
langchain-community>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=1.0.0
# LangGraph - 用于构建 Agent (LangChain 1.2.x 推荐方式)
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ============ LLM & AI ============
# Claude (Anthropic) - 主要 LLM
anthropic>=0.40.0
langchain-anthropic>=0.2.0

# OpenAI - GPT-4/GPT-3.5
openai>=1.0.0
langchain-openai>=0.2.0

# Google Gemini - 可视化方案 (ECharts 代码生成)
google-genai>=1.0.0

# 智谱 AI - GLM-4.7
zhipuai>=2.0.0

# ============ Database ============
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
clickhouse-driver>=0.2.0

# ============ Vector database (可选 - Python 3.14 ARM64 暂不支持) ============
# chromadb>=0.5.0  # 暂时注释：Python 3.14 ARM64 缺少 onnxruntime 支持

# ============ Data analysis ============
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# ============ Scientific & Statistics ============
statsmodels>=0.14.0
scikit-learn>=1.3.0
sympy>=1.12
patsy>=0.5.0

# ============ Excel processing (增强支持) ============
openpyxl>=3.1.0
xlrd>=2.0.0
xlwt>=1.3.0
xlsxwriter>=3.1.0
# pyxlsb>=1.0.0  # Excel 二进制格式支持
# xlwings>=0.30.0  # 需要 Excel 安装，可选

# ============ Visualization ============
# 传统图表库 (保留用于某些场景)
plotly>=5.24.0
matplotlib>=3.9.0
kaleido>=0.2.0

# ECharts (通过 Gemini 3 Pro 生成代码，前端渲染)
# 无需 Python 库，代码由 AI 生成并在前端执行

# ============ Report generation ============
python-docx>=1.1.0
reportlab>=4.2.0

# ============ Web scraping ============
beautifulsoup4>=4.12.0
lxml>=5.3.0
httpx>=0.27.0

# ============ Monitoring ============
loguru>=0.7.0
prometheus-client>=0.21.0

# ============ Infrastructure ============
docker>=7.1.0
redis>=5.2.0
celery>=5.4.0

# ============ Testing ============
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0

# ============ Additional utilities ============
tqdm>=4.65.0
click>=8.1.0
